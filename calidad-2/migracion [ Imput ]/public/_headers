# ═══════════════════════════════════════════════════════════════════════════
# CLOUDFLARE PAGES HEADERS - Optimizado para Seguridad y Performance
# ═══════════════════════════════════════════════════════════════════════════

# ── Seguridad Global (todas las páginas) ──
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=()

# ── Content Security Policy (CSP) ──
/*
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://npmcdn.com https://unpkg.com https://stackpath.bootstrapcdn.com https://cdnjs.cloudflare.com https://fonts.googleapis.com; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://stackpath.bootstrapcdn.com https://cdnjs.cloudflare.com https://fonts.googleapis.com; font-src 'self' https://cdn.jsdelivr.net https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https: blob:; connect-src 'self' https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://stackpath.bootstrapcdn.com https://*.supabase.co wss://*.supabase.co https://script.google.com https://generativelanguage.googleapis.com https://maps.googleapis.com; frame-src 'self' https://maps.google.com https://www.google.com https://www.openstreetmap.org; object-src 'none'

# ── Páginas HTML Dinámicas (sin caché estricto) ──
/*.html
  Cache-Control: public, max-age=0, must-revalidate

# ── JavaScript Crítico (sin caché) ──
/js/auth.js
  Cache-Control: public, max-age=0, must-revalidate

/js/api.js
  Cache-Control: public, max-age=0, must-revalidate

/js/config.js
  Cache-Control: public, max-age=0, must-revalidate

# ── JavaScript No Crítico (caché corto) ──
/js/*.js
  Cache-Control: public, max-age=3600, must-revalidate

# ── Service Worker (caché mínimo) ──
/sw.js
  Cache-Control: public, max-age=0, must-revalidate
  Service-Worker-Allowed: /

# ── Recursos Estáticos (caché largo con immutable) ──
/css/*.css
  Cache-Control: public, max-age=31536000, immutable

/icons/*
  Cache-Control: public, max-age=31536000, immutable

/sounds/*
  Cache-Control: public, max-age=31536000, immutable

# ── Manifest PWA (caché medio) ──
/manifest.json
  Cache-Control: public, max-age=86400, must-revalidate
