/**
 * chat-realtime — DEPRECADA
 *
 * Esta función ya no es necesaria. Las lecturas de CHAT se hacen
 * directamente desde el cliente usando el SDK de Supabase con RLS.
 *
 * El SDK con el token de sesión del usuario aplica RLS automáticamente,
 * eliminando el cold start de esta Edge Function.
 *
 * Mantenida en el repositorio solo como referencia histórica.
 * NO desplegar.
 */
export {};
